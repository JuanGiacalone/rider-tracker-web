info:
  name: GET /health
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}:3001/api/health"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: GET /stores
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/stores"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

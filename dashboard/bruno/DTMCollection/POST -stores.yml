info:
  name: POST /stores
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/stores"
  body:
    type: json
    data: |2-
          {
            "name": "AlemD1",
            "tenantId":2,
            "lat": -38.0000,
            "lng": -57.5500
          }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
